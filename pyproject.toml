[project]
name = "freefox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.2",
    "colorama>=0.4.6",
    "requests>=2.33.1",
]

[project.scripts]
ffox = "ffox.cli:cli"
